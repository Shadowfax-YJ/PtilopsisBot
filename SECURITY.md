# 安全问题与诊断信息

如果仓库已启用 GitHub 私密漏洞报告，请使用该入口。否则先通过不含漏洞细节的 Issue 联系维护者，约定私密反馈方式。不要在公开 Issue 中粘贴 token、Cookie、带签名的下载链接或真实数据包。

机器人默认只监听 `127.0.0.1`，管理命令和 OneBot 连接共用配置中的随机 access token。不要将该端口直接暴露到公网。

`config.toml` 和 `data/` 都是私有运行信息。数据库、日志、文件路径和日报可能包含 QQ 号、昵称、群号、原文件名及上传记录。提交问题时优先提供已脱敏的错误类型、版本和复现步骤；测试样例使用自行构造的数据。
