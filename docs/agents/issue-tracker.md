# 本地票据

本项目使用 `.scratch/qq-group-run-collector/map.md` 和其 `issues/` 下的 Markdown 票据。实现和审查以这些文件及用户最新修正为准，领域术语见根目录 `CONTEXT.md`。

代码完成但真实 QQ 群尚未验证时，记录为 `implemented; live acceptance pending`；真实群收集与删源闭环验收通过后再关闭相应票据。
